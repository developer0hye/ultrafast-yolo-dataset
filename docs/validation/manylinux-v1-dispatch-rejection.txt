Initial source 79a484d: GitHub rejected workflow dispatch before creating a run (HTTP 422). Job-level env used runner.temp, which is not an available expression context there. RUSTUP_HOME, CARGO_HOME and CARGO_TARGET_DIR now derive from RUNNER_TEMP inside the build step shell. No build/test was executed by the rejected dispatch.
Second source 223360a: HTTP 422 before a run was created because the previous edit also removed the existing source-rebuild step CARGO_TARGET_DIR, leaving its env mapping empty. That valid step-level setting is restored; manylinux job paths remain in its shell script.
